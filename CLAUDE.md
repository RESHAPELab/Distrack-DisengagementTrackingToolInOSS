# Claude Code — Project Instructions

## Python command
Always use `python` (not `python3`) when running Python on this machine.
`python3` resolves to a Microsoft Store stub and will fail with exit code 49.

The correct Python is the conda `osslab` environment:
`C:\Users\samut\anaconda3\envs\osslab\python.exe`

To activate manually: `conda activate osslab`
